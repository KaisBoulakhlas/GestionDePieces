<form method="post" action="{{ path('home.range.delete', {id: range.id}) }}"
      onsubmit="return confirm('Etes vous vraiement sûr?')" >
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ range.id) }}">
    <button class="btn btn-sm btn-outline-danger"><i class="material-icons" >delete</i></button>
</form>
