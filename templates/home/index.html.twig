{% extends 'base.html.twig' %}

{% block title %}Tableau de bord atelier{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-5">Tableau de bord - Atelier</h1>
        {% for type, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ type }}" role="alert">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
        <div class="accordion" id="accordionExample">
            <div class="card">
                <a class="btn btn-primary rounded-0" type="button" data-toggle="collapse" data-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                    <div class="card-header bg-danger" id="headingOne">
                        <h2 class="mb-0">
                                Gammes
                        </h2>
                    </div>
                </a>
                <div id="collapseOne" class="collapse" aria-labelledby="headingOne" data-parent="#accordionExample">
                    <div class="card-body">
                        {{ render(controller('App\\Controller\\HomeController::new')) }}
                        <!-- Button trigger modal -->
                        <button type="button" class="btn mb-3 btn-outline-primary" data-toggle="modal" data-target="#addRangeModal">
                            <div class="form-row">
                                <i class="material-icons" >add</i>Gammes
                            </div>

                        </button>


                        <div class="table-responsive">
                            <table class="table table-bordered table-striped table-hover rounded mw-100">
                                <thead class="text-center">
                                <tr>
                                    <th></th>
                                    <th>Libellé</th>
                                    <th>Opérations</th>
                                    <th colspan="3">Actions</th>
                                </tr>
                                </thead>
                                <tbody class="text-center">
                                {% for range in ranges %}
                                    <tr>
                                        <td>{{ range.id }}</td>
                                        <td>{{ range.libelle }}</td>
                                        <td>
                                            <a href="{{ path('home', {id: range.id}) }}" class="btn btn-sm btn-outline-primary"><i class="material-icons" >visibility</i></a>
                                        </td>
                                        <td>
                                            {{ render(controller('App\\Controller\\HomeController::edit', {'id': range.id })) }}
                                            <a href="{{ path('home.range.edit', {'id': range.id}) }}" type="button" class="btn btn-sm mb-3 btn-outline-primary" data-toggle="modal" data-target="#editRangeModal{{ range.id }}"><i class="material-icons" >edit</i></a>
                                        </td>
                                        <td>
                                            {% include('home/_delete_form.html.twig') %}
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
            <div class="card mt-3">
                <a class="btn btn-primary collapsed rounded-0" type="button" data-toggle="collapse" data-target="#collapseTwo" aria-expanded="false" aria-controls="collapseTwo">
                    <div class="card-header bg-danger" id="headingTwo">
                        <h2 class="mb-0">
                                Réalisations de gamme
                        </h2>
                    </div>
                </a>
                <div id="collapseTwo" class="collapse " aria-labelledby="headingTwo" data-parent="#accordionExample">
                    <div class="card-body">
                        Anim pariatur cliche reprehenderit, enim eiusmod high life accusamus terry richardson ad squid. 3 wolf moon officia aute, non cupidatat skateboard dolor brunch. Food truck quinoa nesciunt laborum eiusmod. Brunch 3 wolf moon tempor, sunt aliqua put a bird on it squid single-origin coffee nulla assumenda shoreditch et. Nihil anim keffiyeh helvetica, craft beer labore wes anderson cred nesciunt sapiente ea proident. Ad vegan excepteur butcher vice lomo. Leggings occaecat craft beer farm-to-table, raw denim aesthetic synth nesciunt you probably haven't heard of them accusamus labore sustainable VHS.
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}

