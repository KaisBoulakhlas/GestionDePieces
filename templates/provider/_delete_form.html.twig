<form method="post" action="{{ path('provider.delete', {id: provider.id}) }}"
      onsubmit="return confirm('Etes vous vraiment sûr?')" >
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ provider.id) }}">
    <button class="btn btn-sm btn-outline-danger"><i class="material-icons" >delete</i></button>
</form>
